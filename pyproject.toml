[project]
name = "luxe-ecommerce"
version = "1.0.0"
description = "LUXE Studio E-Commerce Web Application"
requires-python = ">=3.12"
dependencies = [
    "Flask==3.1.1",
    "gunicorn",
]

[tool.vercel]
entrypoint = "run:app"
